# 🖼️ Image Resizer

A simple Python tool that resizes and compresses images using **Pillow** library.  
It helps you reduce image file size easily without losing much quality.

---

## 🚀 Features
- Resize images to custom dimensions  
- Reduce image file size (compression)  
- Works with JPG, PNG, and more formats  

---

## ⚙️ Installation
Make sure you have Python 3 installed.

Then install the required library:
```bash
pip install -r requirements.txt